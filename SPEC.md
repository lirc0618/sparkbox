# MVP 验收规格

本规格用于对本地可运行版本进行最终复核。

## 必须完成

1. 收件箱、全部收藏、项目、今日回顾、收藏详情五个主要区域。
2. 收藏新增、查看、编辑、删除、归档与七阶段状态管理。
3. 项目新增、编辑、删除；收藏可关联多个项目。
4. 浏览器本地持久化，刷新后数据保留。
5. 搜索标题、摘要、备注、正文、标签、来源和项目名称。
6. 每日回顾使用可解释规则，同一天选择稳定，数量为 1–5 条。
7. 支持查看、已使用、加入项目、稍后、不再推荐、跳过；使用时可记录结果。
8. URL 规范化、重复检测；抓取不可用时保留原始 URL 并明确降级。
9. JSON 全量导出、隐私提示、删除二次确认。
10. 桌面与移动端响应式可用。

## 本地验证增强

11. 每行一条内容的批量导入，最多 200 条，并在同一批次内去重。
12. JSON 备份恢复必须先完整校验，失败时不得覆盖当前数据。
13. 提供 14 天验证面板，排除内置示例并显示真实收藏、活跃天数、回顾与旧收藏激活指标。
14. 弹窗内的新增、编辑、设置和批量导入提交按钮必须真实执行，不得被背景关闭事件拦截。

## 不在本地 MVP 范围

- 登录、多用户、跨设备同步和服务端数据库。
- 自动网页抓取、AI 摘要与语义搜索。
- 知识图谱、团队、支付和公开社区。
